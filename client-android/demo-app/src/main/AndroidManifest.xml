<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Note what is NOT here.

        No VpnService declaration, no BIND_VPN_SERVICE, no
        FOREGROUND_SERVICE, no POST_NOTIFICATIONS. All of that lives in
        :vpnlib's manifest and arrives through manifest merge — which is
        the practical difference embedding makes. An integrator adds a
        Gradle dependency, not a manifest they then have to keep in sync
        across upgrades.

        INTERNET comes from :vpnlib too, but the reachability probes in
        this app use it directly, so it is declared here as well for
        clarity. Duplicate declarations merge harmlessly.
    -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!--
        Declared here as well as in :vpnlib because THIS app is the one
        that must request it at runtime on Android 13+. A library cannot
        ask on the host's behalf, and without the request the foreground
        service runs with a notification nobody ever sees.
    -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
